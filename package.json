{
  "name": "leave-approval-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"cd server && python -m uvicorn app.main:app --reload\" \"cd client && npm run dev\"",
    "install:client": "cd client && npm install",
    "install:server": "cd server && pip install -r requirements.txt"
  },
  "workspaces": [
    "client"
  ]
}